import { css } from 'glamor';

export default css({
    backgroundColor: 'var(--green-yellow)',
    border: 'none',
    padding: '10px 0',
    width: '100%',
    color: 'var(--tom-thumb)',
    borderRadius: 20,
    textTransform: 'uppercase',
    fontWeight: 'bold',
    cursor: 'pointer',
});
