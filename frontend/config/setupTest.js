// Importing document-register-element to be able to test
// custom elements in jest, otherwise customElements object
// would not be available in our test environment.
import 'document-register-element'

module.exports = {}
