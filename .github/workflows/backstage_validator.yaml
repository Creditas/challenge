name: Catalog-info.yaml validation

on:
  pull_request:

permissions:
  id-token: write
  contents: write
  statuses: write

jobs:
  validate-catalog-info:
    name: Catalog-info.yaml validation
    uses: creditas/developerhub-actions/.github/workflows/backstage_validator.yaml@master