import React from 'react';

import SearchIconImg from '../../../../assets/images/SearchIcon.png';
import {Icon, IconImage} from './styles';

const SearchIcon = () => (
  <Icon>
    <IconImage source={SearchIconImg} />
  </Icon>
);

export default SearchIcon;
